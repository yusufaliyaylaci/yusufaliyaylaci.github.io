import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yusufaliyaylaci.yaliapp',
  appName: 'YaliApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;